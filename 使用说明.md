# Autodesk Moldflow 2023 模流分析报告 (PPT) 自动化生成系统

> **项目名称**：HYT-MLFXBG (Moldflow Automated PPTX Report Generator)  
> **适用版本**：Autodesk Moldflow 2023 / 2021 / 2019  
> **支持网格**：中性面 (Midplane) / 双层面 (Dual Domain) / 3D 实体网格全自动自适应  
> **开源仓库**：https://github.com/xlzxld/HYT-MLFXBG.git

---

## 📖 一、 系统定位与痛点解决

本系统专为注塑模具与模流分析工程师量身打造，彻底解决传统报告生成中的核心痛点：
1. **告别低效的手动逐项截图**：分析完成后一键全自动提取 30+ 项云图、2D 曲线图与 GIF 动态充填过程；
2. **彻底解决自带报告助手色带遮挡与模糊**：
   - Moldflow 自带报告助手导出强制居中复位，导致模型与左侧彩条标尺（Legend）严重重叠，且分辨率低；
   - 本系统采用 **【方案 B：双图独立导出再智能拼合】**，将色带与超大模型像素级无缝重组，左侧色条清晰锐利，模型满幅震撼呈现；
3. **数据自动回填与高保真模板渲染**：
   - 封面自动替换为 Autodesk 工业冷灰**纯 CAD 实体模型**（无网格、无节点）；
   - 网格统计数据严格继承原模板**空行分明、呼吸感充足**的专业排版；
   - 锁模力与注射压力 XY 图自动定位最高峰，绘制**黄色探针提示框、引线与红点**，并自动将吨位填入表格；
   - 变形结果精细化分离：第 13 页总变形满幅呈现；第 14~16 页 XYZ 分量图**默认勾选并出图**（X/Y 正视，Z 竖直）。

---

## 🌟 二、 核心功能特性全览

### 1. 独创截图标尺方案（默认方案 B，可选方案 A）
* **★ 方案 B：双图独立导出再智能拼合（默认推荐）**：
  - **左侧数据条**：高保真提取结果名称、时间戳、单位、垂直渐变色条及数值刻度，并施加智能锐化；
  - **模型主体**：紧致裁剪四周纯白留白，以最大化比例（占用画布高度 94%）置于画面中右侧；
  - **右下角三维坐标**：透明化叠加真实 XYZ 空间姿态坐标系与旋转角度；
  - **右上角工程标识**：自动提取并拼入当前工程方案名称；
  - **黄金画布比例**：按 `1.58 : 1`（1896×1200）黄金版面输出，与 PPT 幻灯片版芯 100% 严密契合；支持完全后台静默生成。
* **方案 A：视口直接抓取（可选）**：
  - 所见即所得，截取视口屏幕实际呈现画面，保留用户手动摆放的视角与色带布局。

### 2. 全网格类型全自动自适应 (Mesh Adaptive)
* 系统启动时自动检测当前方案网格属性，第 2 页网格统计自动切换表述：
  - **3D 实体网格**：自动输出四面体单元数 (Tetras)、已连接节点数、梁单元数、零件实体体积、流道体积、四面体纵横比统计（最大/平均/最小）等；
  - **双层面 (Dual Domain)**：自动输出三角形数、匹配百分比 (Match ratio，例如 `94.0%`)、相互百分比、表面积、体积、纵横比、边细节（共用边/自由边/多重边）等；
  - **中性面 (Midplane)**：自动输出中性面单元数、表面积、按厚度计算体积、平均厚度、自由边等。
* **文本排版严格对齐**：通过制表位与逐段原位更新，严格保留模板原版各统计区块之间的空白隔离行，版面规整优雅，彻底杜绝所有行挤成一团。

### 3. PPT 前三页全仿真全自动填充
* **第 1 页（封面）**：
  - 表格自动提取当前日期（`YYYY-MM-DD`）、方案名称、产品名称与 Moldflow 版本号；
  - 封面模型图自动替换为**模型本体直出截图** (`solid_model.png`，隐藏节点/梁/结果图后直接截取当前方案模型)，无任何多余网格线或节点遮挡。
* **第 2 页（网格统计）**：
  - 左侧自动裁剪并等比放大展示当前产品网格模型；右侧自动注入精准网格统计参数。
* **第 3 页（材料工艺四联卡片）**：
  - 高保真重绘 Autodesk 官方材质卡片：
    1. **材料基本信息卡片**：包含贸易名称、制造厂商、聚合物代号、材料类型、数据来源、测试日期等 14 项完整参数与顶部选项卡栏；
    2. **推荐工艺参数卡片**：呈现模具温度范围（最低/最高/推荐）、熔体温度范围、顶出温度、最大剪切应力与剪切速率，超标参数红色高亮预警；
    3. **流变粘度曲线**与 **pvT 热力学状态曲线**：调用官方底层接口导出高清图表精准嵌入。

### 4. 第 4 页充填动画 (GIF) 高清优化与防遮挡
* 动态调用 Synergy API `Plot.SetNumberOfAnimationFrames(nFrames)` 导出多达 50~80 帧高细腻动图；
* 配合 Python Pillow 调色板优化，动图体积轻量且播放极其流畅；
* **锁定文本框避让**：右下角“按 Shift+F5 播放”提示文本框已锁定在最佳坐标，并在生成时强制置于顶层，彻底杜绝图片遮挡播放提示。

### 5. XY 曲线图智能拐点探针与参数自动回填
* **第 7 页（注射位置处压力 XY 图）**：
  - 自动识别图像并执行无损白边裁剪；
  - 算法智能定位曲线最高峰拐点，自动绘制 Moldflow 官方风格**黄色探针提示框** (`#ffffc8`)、黑色引线与红色峰值点，准确标注如 `x = 5.092 [s], y = 21.45 [MPa]`。
* **第 9 页（锁模力 XY 图）**：
  - 自动定位锁模力最高峰值点，标注黄色提示框如 `x = 6.224 [s], y = 320.5 [公吨]`；
  - **数据反向回填**：自动读取 CAE 计算出的最大锁模力吨位（如 `320.5T`）及配置的注塑机锁模力（如 `350T`），自动填入幻灯片下方的表格中，严格保持原有微软雅黑 12pt 字体格式。

### 6. 变形结果专业化展示规范
* **第 13 页（变形，所有效应:变形）**：
  - 保留所有效应总变形云图，启用极限满幅自适应算法，最大化放大展现翘曲形态；
* **第 14~16 页（X / Y / Z 方向分量变形）**：
  - 严格保留原有幻灯片页面与右上角标题（如“变形，所有因素: X方向”）；
  - 页面内部图片与小坐标图标全部清空，满足工程评审对分量页面的特定留白需求。

### 7. 稳健的异常防护机制
* **PowerPoint 独占锁自动避让**：当目标 PPT 文件正被外部软件打开时，系统自动捕捉 `PermissionError` 并追加时间戳后缀保存（如 `..._220029.pptx`），绝不崩溃中断；
* **字体多级安全回退**：自动适配各类 Windows 系统的中文字体（SimSun / YaHei / SimHei / Arial），杜绝字体缺失报错。

---

## 🚀 三、 使用方法指南（四种方式任选）

### 方式 1：在 Moldflow 菜单栏运行宏（日常推荐）
1. 在 Autodesk Moldflow 中打开完成分析的方案（Study）；
2. 点击顶部菜单栏：**工具 (Tools) -> 宏 (Macro) -> 播放宏 (Play Macro)**；
3. 选择本项目根目录下的 `AutoReport.vbs` 并点击运行；
4. 脚本将在后台全自动导出所有结果切片并调用 Python 合成报告，生成完毕后自动弹出提示并直接为您打开 PowerPoint 报告！

> 💡 **小技巧（添加一键按钮到顶部快速访问工具栏）**：
> 在 Moldflow 界面顶部的【快速访问工具栏（QAT）】右侧点击小三角 -> **自定义快速访问工具栏** -> 添加宏命令 `AutoReport`，即可在界面顶部获得一个常驻的一键点击按钮！

---

### 方式 2：在 Moldflow 底部命令行直接输入
在 Moldflow 界面最底部的**命令行（Command line）**中直接输入：
```text
mlfx
```
或输入：
```text
AutoReport
```
敲击回车，即可瞬间触发全自动执行。

---

### 方式 3：打开可视化配置中心 (GUI)
当需要调整动图帧数、选择启用额外分析项目（如气穴、熔接痕、冷却管道水温等）或切换方案 A/B 时：
1. 双击运行根目录下的 `config_gui.py`（或运行 `start.bat`）；
2. 界面功能：
   - **截图标尺方案选择**：默认推荐勾选“★ 方案 B：双图独立导出再智能拼合”，亦可单选“方案 A”或“双方案同时生成”；
   - **动画参数**：自由微调充填动画帧数（配置默认 60 帧，推荐 30~80 帧）与播放间隔；
   - **结果项目勾选**：支持在流动、保压、翘曲、冷却、加纤等分类中自由勾选，随时可点击“★ 恢复默认常用 10 项”；
3. 点击底部的 **【★ 保存配置并立即生成报告】**，系统将静默唤醒脚本执行全流程（无黑色控制台黑框弹出）。

---

### 方式 4：纯 Python 命令行独立运行（适用于离线重构或批量生成）
如果已经运行过一次并生成了 `temp/` 数据目录，无需打开 Moldflow 即可在终端秒级重新排版合成 PPT：
```powershell
# 先做环境自检 (依赖/配置/模板), 有问题会给出修复指引
python check_env.py

# 默认采用方案 B 生成单份报告 (脚本默认会自动打开 PPT, 加 --no-open 可禁止)
python core/pptx_builder.py --config report_config.json --data-dir temp

# 独立生成方案 A 报告
python core/pptx_builder.py --config report_config.json --data-dir temp --mode A

# 同时输出 A、B 双方案两份报告 (--output 不能与 BOTH 组合, 两份报告各自命名)
python core/pptx_builder.py --config report_config.json --data-dir temp --mode BOTH

# 无人值守/冒烟测试: 不弹窗 + 指定输出位置 (退出码: 0=成功, 1=环境问题, 2=构建失败)
python core/pptx_builder.py --config report_config.json --data-dir temp --mode B --no-open --output temp\smoke.pptx
```

---

## ⚙️ 四、 配置文件 `report_config.json` 详解

```jsonc
{
  "template_pptx": "templates/Moldflow报告模板.pptx", // 报告母版 PPTX 路径（相对路径相对项目根，建议用相对路径）
  "output_dir": "",                 // 报告输出目录: 留空 = 自动输出到本次模型所在目录（推荐）; 相对路径相对项目根解析
  "open_after_export": true,       // 生成后是否自动用 PowerPoint 打开报告
  "show_gui_before_run": false,    // 运行 VBS 宏时是否先弹出配置界面
  "screenshot_mode": "B",          // 截图方案模式: "B"(默认智能拼合) / "A"(视口抓取) / "BOTH"(同时生成)
  "on_missing_data": "annotate",   // 数据缺失策略: "annotate"(默认, 报告内标注+封面盖章后继续) / "fail"(拒绝生成)
  "animation_settings": {
    "frames": 50,                  // 充填时间动图帧数 (推荐 30~80; 报告内会按此上限抽帧控体积)
    "delay_ms": 80,                // 动图播放每帧间隔毫秒 (数值越小播放越快)
    "optimize_gif": true           // 是否启用 Pillow 调色板深度优化
  },
  "clamp_force_settings": {
    "machine_max_ton": 350         // 注塑机额定最大锁模力吨位 (自动回填 Slide 9 表格)
    // 注: CAE 计算锁模力不再取这里的 cae_max_ton 手工值, 只认 VBS 实机导出的
    // temp/clamp_force_info.json; 该文件缺失时报告会明确标注"数据缺失"
  },
  "plots": [
    // 各分析结果的键名、中英文别名、对应幻灯片页码及开关
    {
      "slide": 4,
      "type": "gif",
      "key": "filling_animation",
      "plot_name": "充填时间",
      "enabled": true
    },
    {
      "slide": 13,
      "type": "image",
      "key": "warpage_all",
      "plot_name": "变形，所有效应:变形",
      "enabled": true
    },
    {
      "slide": 14,
      "type": "image",
      "key": "warpage_x",
      "plot_name": "变形，所有效应:X 方向",
      "enabled": true              // 幻灯片 14~16 默认出图（2026-09-16 定案，可手动改 false）
    }
  ]
}
```

---

## 🆕 五点五、 2026-09 配置界面新能力

1. **结果项页码可改**: 勾选区每个结果项旁有"页码"输入框 (1~16), 可把任意结果
   挪到任意页 (如压力从 P5 改到 P9)。保存时自动查重: 两个勾选项撞同一页会弹窗
   列出冲突, 改完才能保存。注意: 页面版式跟页走 (挪到 P9 就按模板第 9 页版式放)。
2. **从 Moldflow 刷新结果**: 配置界面打开时自动 (及点"⟳ 从 Moldflow 刷新结果"
   按钮) 读取当前方案里的全部结果名 (list_plots.vbs 只读枚举), 默认 13 项按名字
   **一字不差**精确匹配: 匹配到的自动勾选, 匹配不到的不勾并灰显"方案中未找到"。
   结果名找不到时, 把方案里的真名抄进 report_config.json 的 plot_name 即可。
3. **别名匹配已移除**: 程序不再按别名/模糊规则猜结果图 — 只认精确名。历史教训:
   "顶出时的体积收缩率"曾抢匹配真实结果"体积收缩率" (两图并存时导出错图)。

## 六、 模型定向 / 变形图视角 / 封面（2026-09-16 新增）

### 1. 模型定向宏 `OrientModel.vbs`（可选，需要时手动跑）

用途：把模型"转正 + 归零"——你选中的那个面正对屏幕（点"前"视图就是正对着这个面看），
并且这个面的几何中心落在坐标原点 0,0,0。

做法和你手工操作一致：**一次选中全部实体 → 先把面心平移到原点 → 再以 0,0,0 为参考点绕轴旋转**
（网格、流道、水路、节点一起动，不漏、不重复）。

**实体显示（冷流道/热流道/冷却水路/注射位置）**：四个勾选框按**属性**识别（实测：热流道=热流道/热浇口/热主浇道，冷却水路=管道/冷却液入口/冷却回路，注射位置=注射位置节点），
勾选后对应单元会显示在截图里。冷/热/水默认不勾选（不显示）；**注射位置默认勾选**——脚本对它的策略是"**勾了就显示、不勾也绝不动它**"，而且**跑完脚本会把注射位置保持为显示**（不会再出现"跑完图标没了"）。

> **注射位置的确切行为（2026-09-17 定案）**：
> - **每一页按该页的勾选走**：勾了 → 该页显示；没勾 → 该页不显示（与冷/热/水一致）；
> - **跑完软件里的状态**：全局勾选了"注射位置" → 脚本在所有还原动作之后再确保一次显示
>   （复核结果写进 `temp/run.log`：`注射位置最终态: ... 复核 GetTypeVisible=...`）；全局没勾 → 按原样写回；
> - 上一版误开的**文字标注会一次性清掉**：脚本开头统一关闭 N/B/C/NBC 四类标签显示（日志：`标签清理: ...`），
>   之前的"满屏 N99757/B87565"不会再出现。
>
> 注射位置在 Moldflow 里的实体类型是 **NDBC（节点边界条件）**，对应的图层类型开关是 `NBC`。
> 2026-09-17 修好两个坑：① 之前控错了类型（控成普通节点 N）→ 勾选不生效；② 之前应用时机在
> "显示结果图"之前，结果图自身的显示状态会把它盖掉 → 表现为"第一张截图有、后面全没了"。
> 现在改为**每次显示完结果图之后**再应用，并保证跑完仍是显示状态。

**每一页截图可以单独定制**：结果列表里每一行后面有 `显示 冷 热 水 注` 四个小勾（该行没勾选时灰显）。
初始值跟随左侧"截图实体显示"全局设置；**你在哪一行动过小勾，那一行就变成该页专属设置**，之后不再跟随全局。
例：只要"冷却系统分析"那几页显示水路，就在那几页把"水"勾上、全局的"冷却水"保持不勾。

> 唯一没法还原的一件事：脚本抓图前会把**所有图层临时设为可见**（不然你手工隐藏的 CAD 图层抓不到），
> 而 Moldflow 的接口只能读"某图层某类型"的可见性、**读不到"整个图层是否被隐藏"**，所以跑完不会恢复你
> 手工隐藏的那几个图层。习惯用图层隐藏工作的同事，跑完手动恢复一下即可。

**实体"真隐藏"机制（2026-09-17 定案，解决 XYZ 页总带着流道/水路/标记的老问题）**：
Moldflow 的"图层类型开关"（B/C/NBC）**对视口截图不生效**（离屏导出才认，但离屏会丢面），
所以脚本改成你验证过的那条路：**把不该显示的实体临时挪到一个隐藏图层，截完图再原样挪回原图层**。
脚本每次运行都会：建 `MLFX_TempHide_0..3`（建好即隐藏）→ 按该页勾选挪动实体 → 截图 → 全部挪回并删掉临时层。

> ⚠️ 万一运行中途被取消/崩溃，研究里可能残留 `MLFX_TempHide_*` 图层：把它们里面的实体
> 挪回原来的图层、删掉这几个空层即可（日志里会有对应告警）。

**封面图与第 2 页网格图（2026-09-17 修订）**：
- **封面 = 只截 CAD 本体**：按图层类型分离（只留 CAD 面 `S/F/STL/BD`，网格 `T/TE`、流道、水路、节点、边界条件全部隐藏）→ 截出来就是干净的 CAD 本体。
  研究里如果没有 CAD 实体，封面图会接近空白，程序自动回退成"网格模型本体"封面，不会出错。
- **第 2 页"产品网格模型"图 = 反过来**：**只留网格和节点**（`T` 三角 / `TE` 四面体 / `N` 节点），冷热流道、水路（`B` 梁 / `C` 曲线）、CAD 本体（`S/F/STL/BD`）、边界条件标记（`NBC/SBC`）全部隐藏。

**测试数据：研究里没有冷却水路怎么试勾选框**：用 `MakeCoolingCircuit.vbs`（第一版简单版存档是 `MakeCoolingCircuit_v1.vbs`）生成一条测试水路（直径 8、距离 25、3 通道）。
两个版本二选一：`MakeCoolingCircuit.vbs` 会先把残留旧水路清掉再生成（带诊断日志）；`MakeCoolingCircuit_v1.vbs` 是最初那一版，不做清理。
两个都不自动保存，需要时在 Moldflow 里手动保存。**研究没有网格（MeshStatus=New）时生成会半途报错**：实测会在报错前留下半截水路（84 管道 + 2 入口）——请先生成网格再跑宏。

> **顺序很重要**：加冷却水路 = 改模型，Moldflow 会让已有分析结果失效（报告里就没图了）。正确顺序：
> **先建完模型（水路 / 流道 / 定向都做完）→ 再跑分析 → 最后出报告**。反过来做（先分析再加模型）结果必失效，无法避免。

### 1.5 出报告要多久、哪里能提速（2026-09-17）

一次完整运行约 **2 分钟**（实测 135 秒），大头在 Moldflow 自己的渲染，不在程序：

| 环节 | 实测 | 说明 |
|---|---|---|
| 充填动画（GIF） | **31 秒** | 60 帧逐帧渲染，Moldflow 侧成本；**把帧数调到 40 可省约 10 秒** |
| 每页结果图（11 页） | **39 秒** | 每页 ~3.5 秒（显示结果图 + 重绘 + 防陈旧帧等待） |
| 封面 + 网格图 | **14 秒** | 各一次离屏导出（2560×1440）；分辨率调到 1920×1080 会更快 |
| 实体扫描/应用/还原 | **9 秒** | 扫描 1000 个属性类型 ID（为了不漏新类型，保留全扫） |
| Python 生成 PPT | **12 秒** | 方案 A 3.5 秒 + 方案 B 8 秒（含拼图与 GIF 优化） |

已做的两项提速（本次）：每页少一次离屏渲染（直接复用方案 A 的同一张图，约省 10 秒）、
双方案不再重复优化同一个 GIF（约省 2.5 秒，同时避免二次量化伤画质）。

### 2. 截图取景：不再受你的缩放影响（2026-09-17 新增）

以前截图前不动视角，你在 Moldflow 里放大/缩小/平移多少，截出来就是什么样——所以会时好时坏（模型被切一半、或者四周一大片空白）。

现在每张图截图前都会**先"归一取景"**（Fit：把可见模型铺满画面），跟你当前缩放彻底无关；需要时还能留边距。配置界面（高级选项里）三个开关：

| 选项 | 默认 | 说明 |
|---|---|---|
| 截图取景归一 | 勾选 | 每张图截图前先 Fit（推荐一直勾着） |
| 余量缩放 | 1.00 | 1.00 = 铺满；0.90 = 四周多留 10% 边距（怕贴边就调小） |
| 跑完还原运行前的视角与显示 | 勾选 | 跑完把视角（含缩放/平移）和你原来显示的结果图还原回去 |

另外"结果页单页"可以在 `report_config.json` 里用 `view_settings` 单独设 `fit` / `zoom` / `rotation`（XYZ 三页的方向角就在那里），单页设置优先于全局。

**CAD 几何（"CAD 几何"文件夹里的模型本体）：需要在界面里手工转一次**。
经实测确认（Moldflow 的 API 里拿不到 CAD 实体的句柄，几种接口都试过），CAD 无法自动跟随；
宏跑完会在 `temp/orient.log` 里打印**可直接照抄的旋转参数**：

```
参考点 (0,0,0), 轴 (ax, ay, az), 角度 NN.NN 度
```

你在 Moldflow 里选中 CAD 本体（图形窗口点选，或图层面板选中"CAD 几何"下的层），
用旋转工具按上面的参数转一次即可。

另外：报告封面图在探测不到 CAD 实体时会自动改用"网格模型本体"，与旋转后的模型保持一致，不会错位。

步骤：
1. 在 Moldflow 里打开研究，切到**网格显示**，点选一个面（三角形）；
2. 菜单 Tools → Macro → Play Macro，选择 `OrientModel.vbs`；
3. 弹出确认框（会显示面心和要转多少度），点"是"；
4. 跑完在 `temp/` 里留两张对照图：`orient_before.png`（调整前）、`orient_after.png`（调整后）；
5. 宏**不会自动保存**：需要保存时你在 Moldflow 里自己保存；不保存直接关掉，改动不会留到下次。

注意：

- 本操作会修改模型（网格、流道、水路、节点、CAD 几何体一起移动）；
- 若研究里已有分析结果，Moldflow 会让结果失效：**第一次改动可能要等几分钟**（这是结果失效的一次性处理，不是卡死，别中断）；处理完之后同规模的操作只要 1 秒左右；
- 宏**不会自动保存**，需要保存时你在 Moldflow 里自己保存；
- 建议先在**副本研究**上试一次（复制方法：Study Tasks 面板右键研究 → Duplicate）。

想调朝向改 `report_config.json` 里的 `orient_settings`：

| 字段 | 默认 | 说明 |
|---|---|---|
| `target_axis` | `"Z"` | 面要朝向的轴（点"前"视图正对该面） |
| `flip_normal` | `false` | 如果看到的是背面，改成 `true` 再跑一次 |

运行日志：`temp/orient.log`。

### 2. 变形图视角（X / Y / Z 分量图）

`report_config.json` 里每个变形图自带 `view_settings`，旋转角（单位：度）可手动改：

- `warpage_x` / `warpage_y`：默认 `[0, 0, 0]`
- `warpage_z`：默认 `[-90, 0, 0]`

截图完成后会自动恢复你原来的视角；其他结果图不受影响。

### 3. 封面模型图（PPT 第一页）

现在封面图只显示 **CAD 图层**（一般叫「CAD几何」），其他图层全部隐藏后再截图，
截完自动恢复原状（不会再混入流道、水路、网格和节点）。CAD 图层由程序自动识别
（先找 CAD 体/面，再找 CAD 曲面）。

如果整个研究里没有 CAD 几何（例如 Fusion 网格的工程），程序会自动改成
"只显示模型本体"，不会出空白封面。

## ❓ 五、 常见问题排查与技术答疑 (FAQ)

### Q1: 运行后提示“无法连接到 Autodesk Moldflow Synergy”？
- **原因**：当前电脑未启动 Moldflow，或者有多个 Moldflow 实例导致 COM 对象获取异常。
- **解决**：请先启动 Moldflow 并打开对应的分析工程方案（`.sdy` 文件），确保处于模型可见的激活状态，然后再运行脚本。

### Q2: 生成报告后提示“File locked by PowerPoint, saving as: ..._HHMMSS.pptx”？
- **原因**：之前的同名 PPT 报告正处于打开状态，Windows 系统对其施加了文件读写独占锁。
- **解决**：这是系统的自动安全保护机制，脚本会自动追加当前时间戳保存为新文件，绝不报错丢失数据；若要直接覆盖原文件，请先在 PowerPoint 中关闭旧报告。

### Q3: 为什么有的结果在 PPT 中没有图片？
- **情况 A**：对于幻灯片 14~16（XYZ 方向变形），这是根据工程交付规范专门设计的——**保留幻灯片页面与右上角标题，清空内部图片**。
- **情况 B**：如果是其他幻灯片未出现图片，请检查当前 Moldflow 方案中该项分析是否实际计算完成（例如是否运行了翘曲分析，或未配置冷却回路导致缺少水温结果）。

### Q4: 想要在 PPT 中追加新的分析结果页面？
- 在 `config_gui.py` 界面中勾选需要的额外结果（如“气穴”、“总体温度”等），或者在 `report_config.json` 中将对应结果项的 `"slide": null` 设为启用，脚本会自动以标准模板母版追加新页面并在正中央高清呈现。
